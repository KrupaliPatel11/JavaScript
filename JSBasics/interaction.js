// Skipped Topic
// script tag
// script tag attributes like type and language
// add external script using src
// code structure
// use-strict using this code will run in strict mode like we cannot use undeclared variables
// use-strict in console
// variable- let, var, const
// Data type - will refer in seperate module


// Interaction-alert
let msg = "Hey, I am krupali";
alert(msg);

// prompt
let num = +prompt("Enter any number", "");
alert(num);

// confirm
let check = confirm("Are you sure ?");

// break in loop
let sum = 0;
while(true) {
let num = +prompt("Enter Any Number", ""); 
    if(!num) break;
    sum += num;
}
console.log(sum);



