let value = true;
console.log(typeof(value));

value = +value; /* to convert in to number */
value = Number(value);  /* also use to convert in to number */
value = String(value);  /* to convert in to string */
console.log(typeof(value));